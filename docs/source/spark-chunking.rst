Chunking
========

Another common use case is breaking an array into chunks, and then performing operations over the subarrays. Bolt uses this kind of chunking under the hood during it's swap operation (read ) perform this